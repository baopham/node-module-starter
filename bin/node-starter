#!/usr/bin/env node

const program = require('commander')
const pkg = require('../package.json')

program
  .version(pkg.version)
  .command('setup', 'Setup all the common tools, files and scripts')
  .parse(process.argv)
